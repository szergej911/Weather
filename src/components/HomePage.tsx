import React, { FC } from "react";
import Nav from "./Nav";
import Main from "./Main";

const HomePage: FC = () => {
  return (
    <div>
      <Nav />
      <Main />
    </div>
  );
};

export default HomePage;
